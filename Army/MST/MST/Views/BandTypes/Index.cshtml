@model IEnumerable<MST.Models.BandType>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Admin.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.btNameType)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.btNameType)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.btBandTypeID }) |
            @Html.ActionLink("Details", "Details", new { id=item.btBandTypeID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.btBandTypeID })
        </td>
    </tr>
}

</table>
